#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - entry point
 * Return: always returns a value
 */
int main(void)
{
int n;
for (n = 0; n < 10; n++)
{
	printf("%d", n);
}
printf("\n");
return (0);
}
